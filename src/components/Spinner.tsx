// The gold/purple spinner from the design system. Shown beside buttons during
// slow actions so it is clearly visible.
export const Spinner = (): React.ReactElement => (
  <span className="spinner" aria-hidden="true" />
);